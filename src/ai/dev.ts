import { config } from 'dotenv';
config();

// Flows are now defined within server actions, so they dont need to be imported here for dev.
// This file is still useful for other potential dev-only AI setup in the future.
